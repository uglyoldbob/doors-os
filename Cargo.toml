
[workspace]

members = [ "bochs-debug",
    "builder", "doors-analyzer", "imager", "kernel-stack-analysis",
    "network_test",
]
exclude = [
    "kernel",
    "user",
    "rust",
]

[profile.dev]
opt-level=3

[profile.release]
debug = true
