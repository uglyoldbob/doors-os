This directory contains the bare bones of the uDis86 library.
These are the external functions that need to be written to allow it to work with Doors.
Some of the native source files needed to have (#include <stddef.h>) added to them.

sprintf
__assert
fgetc
vsprintf
_impure_ptr
