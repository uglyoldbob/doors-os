[package]
name = "doors_macros2"
version = "0.1.0"
edition = "2021"

[dependencies]
futures = { version = "0.3.31", default-features = false, features = ["alloc"] }
pin-project-lite = "0.2"
spin = "0.9.8"

[features]
default = ["backtrace"]
backtrace = []